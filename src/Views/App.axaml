<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SimpleSimulator.Views.App"
             x:DataType="viewModels:NavigationViewModel"
             xmlns:views="clr-namespace:SimpleSimulator.Views"
             xmlns:viewModels="clr-namespace:SimpleSimulator.ViewModels"
             RequestedThemeVariant="Dark">
    <Application.Styles>
        <FluentTheme />
    </Application.Styles>
    <!-- <Application.Resources>
        <StyleInclude Source="avares://Avalonia.Themes.Fluent/FluentDark.xaml"/>
    </Application.Resources> -->
    <Application.DataTemplates>
        <DataTemplate DataType="viewModels:MainMenuViewModel">
            <views:MainMenuView />
        </DataTemplate>
        <DataTemplate DataType="viewModels:SimulationViewModel">
            <views:ProjectileMotionView />
        </DataTemplate>
    </Application.DataTemplates>
    <!-- <Application.MainWindow>
        <Window DataContext="{Binding NavigationViewModel}"
                Width="1024"
                Height="768"
                Title="Simple Simulator">
            <ContentControl Content="{Binding CurrentView}"/>
        </Window>
    </Application.MainWindow> -->
    <!-- <Grid>
        <ContentControl Content="{Binding CurrentView}"/>
    </Grid> -->
</Application>